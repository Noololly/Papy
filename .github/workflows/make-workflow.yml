name: Build-Binary

on:
  workflow_dispatch:
  
jobs:
  build:
    name: Build Binary
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
